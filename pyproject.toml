[project]
name = "tempo"
version = "0.0.1"
description = "Agent for the Pokémon TCG AI Battle Challenge"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"
