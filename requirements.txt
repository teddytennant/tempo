# Dev / test (pure-Python, runs the contract tests against the mock engine)
pytest>=8.0
numpy>=1.26

# Submission/eval (install when the Kaggle engine is available):
#   kaggle
#   kaggle-environments==1.30.1
# Training (install for Stage 3+, see requirements-train.txt):
#   torch, onnx, onnxruntime
